import os, sys
Import('env')
env = env.Clone()

cpps = Glob("*.cpp")
lib = env.SharedLibrary(target = env["build_lib_path"][0] + "util",
        source = cpps)
SConscript("test/SConscript")

#env.Install(env["build_lib_path"], lib)
#header_install_dir = [env['install_head_root']  + "dom/"]
#env.Alias("install", env.Install(header_install_dir, Glob("*.h")))
#env.Alias("install", env.Install(env["install_lib_root"], lib))
